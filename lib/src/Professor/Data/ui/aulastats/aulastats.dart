import 'package:flutter/material.dart';

class AulaStatsPage extends StatelessWidget {
  const AulaStatsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
